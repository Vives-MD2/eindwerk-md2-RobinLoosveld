<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Thunderstruck.UI.ViewModels;assembly=Thunderstruck.UI"
             x:Class="Thunderstruck.UI.Views.Project.Forecast.ForecastToday">
    <ContentPage.BindingContext>
        <viewModels:ForecastTodayViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <Label Text="Location"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />

            <Entry x:Name="eEnterLocation" Placeholder="Enter a location" Text="{Binding EnteredLocation}" />
            <Button x:Name="btnToday" Text="Today" Clicked="BtnToday_OnClicked" VerticalOptions="CenterAndExpand" Command="{Binding GetCurrentWeatherCommand}" />
            <ListView x:Name="lstToday"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>