<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Thunderstruck.UI.ViewModels;assembly=Thunderstruck.UI"
             xmlns:user="clr-namespace:Thunderstruck.UI.ViewModels.User;assembly=Thunderstruck.UI"
             x:Class="Thunderstruck.UI.Views.Project.UserInfo.LoginPage"
             BackgroundImageSource="backgroundlogin.jpg">

    <ContentPage.BindingContext>
        <user:LoginViewModel/>
    </ContentPage.BindingContext>
    
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <StackLayout Padding="0,20,0,0" Grid.Row="0">
            <Label TextColor="White" FontFamily="sans-serif" FontAttributes="Bold" FontSize="40"  x:Name="lblLogin"  Text="THUNDERSTRUCK" HorizontalOptions="CenterAndExpand"/>
        </StackLayout>
            <StackLayout Grid.Row="1" Padding="10" VerticalOptions="End">
            <Label HorizontalOptions="Center" Text="Connect with" TextColor="White" FontSize="Title"/> 
            <ImageButton x:Name="btnLogin" Scale="0.75" CornerRadius="15" BackgroundColor="#F205702B" Source="Spotify_Logo_CMYK_White.png" Command="{Binding LoginSpotifyCommand}"/>
        </StackLayout>
    </Grid>
</ContentPage>